pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "TacticsFlame"

include(":core")
include(":android")
include(":desktop")
